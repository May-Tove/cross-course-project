import { data } from "./jackets/products.js";

const detailContainer = document.querySelector(".detail-result");

/*const productDetails = data.find((productDetails) => {
  if (productDetails.id === 2) {
    console.log(productDetails);
  }
});*/
